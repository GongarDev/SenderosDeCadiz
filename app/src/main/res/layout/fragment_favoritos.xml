<?xml version="1.0" encoding="utf-8"?>

<!-- Este fragment se visualiza en principalmente en el navigation si se el elije el ítem del menú
del navigationUI correspondiente. Podemos ver que muestra un recyclerView, en este caso de Objetos Sendero
 para la sección de favoritos-->

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/background_main"
    tools:context=".fragments.FavoritosFragment">

    <TextView
        android:id="@+id/NoFoundFavoritosTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:textAlignment="center"
        android:textSize="20sp" />

    <ImageView
        android:id="@+id/progressBarFavoritos"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_gravity="center"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewFavoritos"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical"/>

</FrameLayout>